# =============================================================================
# APP CONFIGURATION
# =============================================================================

# App URL - Used for magic link redirects and auth callbacks
# Local development: http://localhost:3000
# Production: https://auth.helvety.com
# NOTE: Make sure this URL is in your Supabase Redirect URLs allowlist
# (Supabase Dashboard > Authentication > URL Configuration > Redirect URLs)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from: Supabase Dashboard > Project Settings > API

# Project URL - RESTful endpoint for database access
NEXT_PUBLIC_SUPABASE_PROJECT_URL=https://your-project-ref.supabase.co

# Anon Key (publishable) - Safe for browser use with Row Level Security
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here

# Service Role Key (secret) - Bypasses RLS, server-side only
# WARNING: Never expose this key to the client!
SUPABASE_SECRET_KEY=your-service-role-key-here
